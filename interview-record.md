# 问题

## 个人问题

### 常规问题

#### 封装了哪些hooks，封装思想

1. useWebsocket
2. useInput等等

常规思想为：返回更改方法及相关依赖，并提供更改依赖后触发状态更新的方法等等。

### 性能优化

#### 首屏性能优化

如果要考虑做首屏性能优化就需要清晰页面加载的整个流程：

1. 构建请求行将请求行通过进程间通信（IPC）发送给网络进程

   1. 缓存策略

      强缓存通过响应字段cache-control和expires

      协商缓存两种方式为：

      请求：if-modified-since 响应：last-modify

      请求：if-none-match 响应：etag

      区别：etag是根据文件内容生成的hash串，只有当内容产生变更时才会生成新的etag，不同服务器对应的etag的值是不同的。相比于if-modified-since，etag具有几个优点：针对无内容变更但是会更新修改时间的文件还是保存原缓存内容，能够对秒级以内多次变更的文件生成新的etag，有些资源文件无法获取资源修改时间

      缓存位置：service worker、memory cache、disk cache、push cache

      memory cache为内存缓存 速度 > disk cache 磁盘缓存

      大文件的js css会被放到磁盘缓存中，反之会放到内存缓存中

      内存使用率较高时会直接放到磁盘缓存中

      1. 由于采用版本号更新方式，所以对静态资源文件使用本地浏览器和cdn30天强缓存，腾讯云cos默认支持etag，鉴权资源及其他utils文件本地缓存10分钟，cdn缓存30天，脚本上传通过接口手动刷新

2. 网络进程需要先进行DNS域名解析，将域名转化为ip地址

   域名查找路径：本地域名服务器、根域名服务器、顶级域名服务器

   优化方法：

   1. dns负载均衡

      用户请求时，根据用户所在地理位置和服务器负载量选择一个最合适的响应服务器

   2. dns缓存

      DNS域名解析缓存：浏览器缓存、系统缓存、路由器缓存、运营商缓存、根域名服务器缓存、顶级域名服务器缓存、主域名服务器缓存

   3. dns prefetch

      对dns进行预解析

      使用注意事项：

      1. 仅对跨域域名有效
      2. 可以使用preconnect提高效率，但是注意仅对关键请求使用，不要过度使用

3. 获取完ip地址之后，根据当前http版本以及当前请求数量决定是否能够发起请求

   1. http1.1版本最多建立6个tcp连接，超过需要等待

   2. 优化方向为升级为http2，http2的优点有

      1. 头部压缩
      2. 服务端推送
      3. 二进制传输
      4. 请求优先级

   3. 三次握手

      由于双方都要确保有接受和发送能力，且避免接收没必要的请求所以不能为两次

4. 服务端接受请求信息之后进行处理，判断协商缓存

   1. 合理利用协商缓存
      1. 协商缓存主要有两种使用方式：if-none-match last-modified e-tag 
      2. 两种方式的区别为：有时我们会更新文件但是文件本身无任何变化，这时候etag是不变的

5. 将内容缓存

6. 客户端接收到内容之后对接收到的html进行解析

   解析过程为：构建dom树、样式计算、生成布局树。

   渲染过程的优化方向：

   1. 由于js会阻塞html解析，所以对非首屏必须的js文件添加async或者defer或者放在body底部
   2. 又由于当js方有css文件时，这时css会阻塞js的解析所以需要根据必要性合理规划文件的位置
   3. 利用分包优化首屏文件大小，减少多个文件的加载

7. 解析完成后进入渲染过程

   渲染过程：建立图层树、生成绘制列表、生成图块并栅格化、显示器显示内容

   1. 生成图层树：显示合成和隐式合成

      显示合成为：

      1. HTML根元素本身就具有层叠上下文。
      2. 普通元素设置**position不为static**并且**设置了z-index属性**，会产生层叠上下文。
      3. 元素的 **opacity** 值不是 1
      4. 元素的 **transform** 值不是 none
      5. 元素的 **filter** 值不是 none
      6. 元素的 **isolation** 值是isolate
      7. **will-change**指定的属性值为上面任意一个。(will-change的作用后面会详细介绍)

      隐式合成为：

      `层叠等级低`的节点被提升为单独的图层之后，那么`所有层叠等级比它高`的节点**都会**成为一个单独的图层。

      所以要注意的问题：层级爆炸

   渲染过程时的优化方向：

   1. 减少回流的操作

      触发回流的操作有

      1. 一个 DOM 元素的几何属性变化，常见的几何属性有`width`、`height`、`padding`、`margin`、`left`、`top`、`border` 等等, 这个很好理解。
      2. 使 DOM 节点发生`增减`或者`移动`。
      3. 读写 `offset`、`scroll`和`client`属性的时候，浏览器为了获取这些值，需要进行回流操作。
      4. 调用 `window.getComputedStyle` 方法。

   2. 减少重绘的操作

      当 DOM 的修改导致了样式的变化，并且没有影响几何属性的时候，会导致`重绘`(`repaint`)。

   3. 如果样式修改较多，多用样式合成和gpu加速

      利用 CSS3 的`transform`、`opacity`、`filter`这些属性就可以实现合成的效果，也就是大家常说的**GPU加速**

#### 代码性能优化

1. 使用suspense+react.lazy实现懒加载
2. 通过webpack配置按需加载
3. 善用memo、PureComponent、shouldComponentUpdaet等api
4. 减少渲染次数，对不必产生渲染的state利用useRef做数据缓存
5. 虚拟列表、分片渲染等
6. tree shaking删除冗余代码
7. 善用useCallback、useMemo等hooks

#### 其他优化

1. external大文件如antd等
2. 使用dayjs替换moment，或者剥离moment的local语言包
3. 对node_modules进行分包
4. 使用动态导入配合页面路由做按需加载
5. 在页面加载前引入loading动画 或者 骨架屏

#### 优化前后差异

1. 通过bundle-analyze可以看到打包大小有明显减少由原来600+k降到300多一点
2. 首屏加载从3s+降到2s以内

#### 优化的量化指标

1. FCP：从开始加载到页面中第一个元素被渲染之间的时间 （3.5s）

   fast：0-2s 

   moderate：2-4s 

   slow:：> 4s

2. LCP：标准视口内可见的最大内容元素的渲染时间（7.5s）

   fast：0-2.5s

   moderate：2.5-4s

   slow: >4s

3. FID：用户第一次与页面交互到浏览器能够响应这一交互的时间

   fast：<100ms

   moderate：100-300ms

   slow：>300ms

4. TTL：页面到达第一次完全可交互的时间点 （850ms）

#### devtools排查页面性能瓶颈

1. 通过network面板查看页面资源请求相关，这里会产生阻塞的情况有
   1. 资源请求个数多 队列时间长
   2. js资源阻塞时间过久
   3. 接口响应时间过久
2. 通过performance面板查看具体性能问题
   1. 每个资源各项具体时间
   2. 每个资源持续时间
   3. 各个性能指标时间
3. 通过lighthouse面板查看可视化的性能问题
   1. 各项性能分数如SEO、performance等等
   2. performance会包含每项性能指标的具体时间，如：ttl，fcp，lcp等等
   3. 包含优化建议和方向

## 安全问题

会经过第三方安全公司检测，针对渗透测试报告配合运维、后端做响应安全措施，例如使用滑动验证，后端对富文本内容转换等等。

### csrf

跨站请求伪造攻击：黑客诱导用户点击链接，打开黑客的网站，然后黑客利用用户目前的**登录状态** 发起请求，主要攻击手段就是发送请求。

防范措施

1. 利用cookie的SameSite属性
2. 验证来源站点：origin和referer，注意这两者可以伪造
3. 利用csrf token

### xss

跨站脚本攻击：在浏览器中恶意执行脚本，获取用户信息进行操作。

主要用于

1. 获取cookie
2. 监听用户行为，比如监听用户输入账号密码操作并发送到黑客服务器
3. 修改dom伪造登录表单
4. 在页面中生成浮窗广告

通常情况下，xss攻击有三种常见类型：存储型、反射型和文档型

应对方法：

1. 对用户输入进行转义
2. 利用csp：内容安全策略，服务器决定浏览器加载哪些资源
   1. 限制其他域下的资源加载
   2. 禁止向其他域提交表单
   3. 提供上报机制，及时发现xss攻击
3. 利用httponly

## react

### 版本

大部分的项目中主要使用的是umi，umi有2.x、3.x、4.x的版本，有一些很老的项目是直接接入的react使用的是react15版本

1. Umi2
   1. react router 4
   2. react 16.8+
   3. webpack 4
   4. Node 8.10+
2. Umi3
   1. react 16.8+
   2. node 10+
   3. webpack 4
   4. react router 5
3. Umi4
   1. react18
   2. node 14+
   3. react router 6
   4. webpack 5

### react各版本区别

#### 15和16

1. 15架构部分为Reconciler和Renderer，会采用递归更新子组件，一旦开始更新，中间无法暂停。16及以后的架构都为Scheduler、Reconciler、Renderer三部分，更新流程从递归变为可中断的循环渲染。
2. 16版本引入fiber架构，最主要的变更
3. 16.8引入hooks

#### 16和17

1. 新的jsx的转换，16版本会借助babel-loader编译jsx为react.createElement()，17会从package中引入新的入口函数并调用
2. 事件代理更改：16版本的事件都是代理到document上，17会代理到rootNode上，用来兼容多个子应用的问题
3. 事件池改变，在异步事件里不需要通过e.presist()也可拿到e，事件代理机制变更

#### 17和18

1. 18版本自动批处理更新，在原来的版本中，异步回调或者原生dom事件里的批量更新会失效。在18版本的这些场景中，批量更新仍然会保持正常，但是这一点属于broken change所以如果想要退出批量更新需要通过flushSync

2. 对Suspense更新使用新的处理方式，不再跳过fallback为null的Suspense

3. 新增一些API

   1. useId
   2. useSyncExternalStore
   3. useInsertionEffect

4. 并发更新开启的不同

   1. 在react18之前通过开启并发模式开启并发更新。在react18中：通过react.createRoot开启并发模式，但是开启并发模式并不代表开启并发更新，而是以是否使用并发特性作为是否开启并发更新的依据。所以，在react18中不再有多种模式
   2. **并发特性**是指开启**并发模式**后才能使用的新特性，如
      1. useDeferredValue
      2. useTransition
   3. 从架构层面来讲，react当前一共有以下两种架构：
      1. 采用**不可中断的递归**方式的**Stack Reconciler**(老架构)
      2. 采用**可中断的遍历**方式的**Fiber Reconciler**(新架构)
   4. 当前市面上所有React版本有四种情况
      1. 老架构（v15及之前版本）
      2. 新架构，未开启并发更新，与情况1行为一致(v16、v17默认属于这种情况)
      3. 新架构，未开启并发更新，但是启用了并发模式和一些新功能(比如**Automatic Batching**，v18默认属于这种情况)
      4. 新架构，开启并发模式，开启并发更新

   [一文让你搞懂React18新特性及其实现](https://juejin.cn/post/7118671121251057700)

   

#### react架构结论

1. **并发更新**的意义就是**交替执行**不同的任务，当预留的时间不够用时，React将线程控制权交还给浏览器，等待下一帧时间到来，然后继续被中断的工作
2. **并发模式**是实现**并发更新**的基本前提
3. **时间切片**是实现**并发更新**的具体手段
4. 上面所有的东西都是基于**fiber**架构实现的，**fiber**为状态更新提供了可中断的能力

关于fiber架构，有三层具体含义：

1. 作为`架构`来说，在旧的架构中，`Reconciler（协调器）`采用递归的方式执行，无法中断，节点数据保存在递归的调用栈中，被称为 `Stack Reconciler`，stack 就是调用栈；在新的架构中，`Reconciler（协调器）`是基于fiber实现的，节点数据保存在fiber中，所以被称为 `fiber Reconciler`。
2. 作为静态`数据结构`来说，每个fiber对应一个组件，保存了这个组件的类型对应的dom节点信息，这个时候，fiber节点就是我们所说的`虚拟DOM`。
3. 作为动态`工作单元`来说，fiber节点保存了该节点需要更新的状态，以及需要执行的副作用。

## react-router

### 4和5



### 5和6

1. 由原来的Switch变为Routes
2. 路由重定向方式修改：删除原有Redirect标签，使用Navigate标签
3. 原来Route标签里的component改为element，且内容为组件标签
4. 使用useRoutes hook替换原有的自定义路由表
5. 扩展了更多的hooks
6. 路由跳转方式变更，由原来的history.push变成navigate()

## react状态管理

### redux

通过dispatch一个action，然后更改state，触发view更新。

reducers接收一个state和action，返回一个新的state。

crateStore接收reducers，返回一个store。

三大原则：

1. 数据唯一性原则：全局只有一个store
2. 保持只读状态：state是只读的
3. 数据更改只能通过纯函数进行

主要实现原理为：通过单例模式，内部维护一个state，通过闭包持久化这个state，通过dispatch方法派发动作更新state，同时内部使用发布订阅模式，让外部添加监听函数，监听state的变化。

[Redux/react-redux/redux中间件设计实现剖析](https://juejin.cn/post/6844904036013965325#heading-0)

[react 状态管理之redux 使用与实现原理](https://juejin.cn/post/7242532081288134715)

[一文总结redux、react-redux、redux-thunk、redux-sage | 掘金技术征文-双节特别篇](https://juejin.cn/post/6880011662926364679#heading-17)

### react-redux

减少重复代码的书写。

在redux的基础上封装数据变化的订阅和更高效的派发action获取state，以及更优雅的用connect代替Selector。

主要实现原理是通过react的Context和Provider。

[react 状态管理之 react-redux 使用与实现原理](https://juejin.cn/post/7243241586569543740)

### redux-thunk

redux中间件，支持在reducer中发送异步请求。

### redux-saga

本身为用在redux中发送异步请求的中间件，主要借助生成器函数和generator。

### mobx

借助Object.definepropertyies或者proxy响应式的更新数据，且与redux不同的是，他的数据思想为mutable，所以在需要更改数据结构的情况下用mobx并不是一个好的选择。

与redux不同的是：

1. 数据流：Redux遵循单向数据流的模式，所有的状态变化都通过派发动作（Actions）来触发，然后由纯函数的“减员”（Reducers）进行处理并更新状态。
2. 中央存储：Redux使用一个单一的中央存储（Store）来保存整个应用程序的状态，并且状态是不可变的。
3. 可预测性：Redux的设计目标之一是提供可预测的状态管理。通过严格约定的数据流和纯函数的操作，可以更容易地追踪和调试状态变化。
4. 插件生态系统：Redux拥有强大的生态系统，有许多第三方插件和中间件可用于扩展其功能。

### recoil

1. 数据流方式：Recoil采用了原子（Atom）和选择器（Selector）的模型，它允许直接读取和更新状态，而无需通过中间件或reducers。Recoil使用了基于订阅的更新模型，当状态变化时，相关组件会自动重新渲染。Redux则使用了单一的全局状态存储，通过actions和reducers来管理和更新状态。
2. 状态管理复杂性：Recoil旨在简化状态管理，减少了Redux中的一些繁琐的模板代码和概念，使得状态管理更加直观和自然。Recoil的设计目标是提供一种轻量级、无样板代码的状态管理解决方案，尤其适用于中小型应用。而Redux则提供了丰富的中间件和工具，适用于复杂的应用场景和大型项目。
3. 异步支持：在处理异步操作时，Recoil提供了更为简洁的语法和API。您可以直接使用异步函数或返回Promise的函数来计算Selector的值，并且Recoil会自动处理异步操作的状态和更新。Redux则需要使用中间件（如redux-thunk、redux-saga等）来处理异步操作，这会增加一些额外的复杂性和概念。
4. 生态系统和社区支持：Redux是一个非常成熟和广泛使用的状态管理库，在React生态系统中有大量的相关工具、中间件和社区支持。Redux拥有丰富的插件和工具生态系统，使得开发者可以轻松地扩展和定制其功能。相比之下，Recoil是一个相对较新的状态管理库，虽然在React社区中获得了一定的关注，但其生态系统和社区支持还不如Redux那么成熟。

选择使用Recoil还是Redux取决于具体的应用需求和个人偏好。如果您正在构建一个较小规模的React应用，并且希望简化状态管理的复杂性，那么Recoil可能是一个很好的选择。如果您需要处理复杂的应用逻辑、异步操作或与现有的Redux代码库集成，那么Redux可能更适合您的需求。

## js模块化

### CommonJs

常用于：node、服务端、webpack

特点：同步/运行时加载，磁盘读取速度快

使用方法：使用module.exports或者exports导出，用require加载模块

### AMD

用于浏览器端

特点：

1. 异步加载

   因为面向浏览器端，为了不影响渲染

2. 依赖前置

   所有的依赖必须写在最初的依赖数组中，速度快，但是会浪费资源，预先加载了所有依赖不管你是否用到

使用方法：使用define定义模块，使用require使用模块

### UMD

是通用模块定义的缩写。umd是amd和cjs的一个糅合。

先判断是否支持node的模块，支持就使用node；再判断是否支持amd的加载方式，支持就使用amd。

### CMD

与AMD很相似，不同点在于AMD推崇依赖前置、提前执行，CMD推崇依赖就近，延迟执行。

使用方法：使用define定义模块，使用SeaJs的use使用模块

### ES Module

设计思想是静态化，在编译时就能确定模块的依赖关系。

### AMD和CMD的区别

1. 对于依赖的模块，AMD是提前执行，CMD是延迟执行
2. AMD推崇依赖前置，CMD推崇依赖就近
3. AMD的API默认是一个当多个用，CMD的API严格区分，推崇职责单一

### ES6模块与CommonJs的区别

1. CJS输出的是值的拷贝，ES输出的是值的引用
2. CJS是运行时加载，ES是编译时输出接口
3. CJS的require是同步加载模块，ES的import是异步加载，有一个独立的模块依赖的解析过程

## 微前端

### 什么是微前端

微前端架构具备以下几个核心价值：

- 技术栈无关
  主框架不限制接入应用的技术栈，微应用具备完全自主权
- 独立开发、独立部署
  微应用仓库独立，前后端可独立开发，部署完成后主框架自动完成同步更新
- 增量升级在面对各种复杂场景时，我们通常很难对一个已经存在的系统做全量的技术栈升级或重构，而微前端是一种非常好的实施渐进式重构的手段和策略
- 独立运行时
  每个微应用之间状态隔离，运行时状态不共享

微前端架构旨在解决单体应用在一个相对长的时间跨度下，由于参与的人员、团队的增多、变迁，从一个普通应用演变成一个巨石应用([Frontend Monolith](https://www.youtube.com/watch?v=pU1gXA0rfwc))后，随之而来的应用不可维护的问题。这类问题在企业级 Web 应用中尤其常见。

### 为什么使用微前端

微前端解决方案 可 提供如下特性：

- 单个前端部分可独立开发、测试和部署；
- 无需重新构建即可添加、移除或替换单个前端部分；
- 不同的前端部分可使用不同的技术构建。

由此可见，微前端主要实现了解耦。

微前端是一种软件架构的概念，它旨在将前端应用程序拆分为更小的、相互独立的部分，这些部分可以独立地开发、测试和部署。每个部分都可以由不同的团队开发和维护，从而提高了应用程序的可维护性和可扩展性。

**对于我们来说，有三点：**

1. **老项目是react16.8以下的，不再继续维护，新项目想要新的技术架构。** 旧的系统不能下，新的需求还在来。
   没有一家商业公司会同意工程师以单纯的技术升级的理由，直接下线一个有着一定用户的存量系统的。而你大概又不能简单通过 iframe 这种「靠谱的」手段完成新功能的接入，因为产品说需要「弹个框弹到中间」
2. **原来的项目很大，开发、打包、发布都很慢，体验很差，使用微前端可以拆分各个独立的业务**
3. **部分业务项目外包给外部团队开发，需要支持独立打包部署，可以很好解耦**

### 为什么不用iframe

1. 浏览记录无法自动被记录，浏览器刷新，状态丢失、后退前进按钮无法使用。
2. 嵌套子应用弹窗蒙层无法覆盖全屏
3. 页面通信比较麻烦，只能采用postMessage方式。
4. 每次子应用进入都需要重新请求资源，页面加载速度慢。

**强调一下，目规模小、数量少的场景其实不建议使用微前端。**

### 微前端优缺点

微前端的优点包括：

1. 灵活性：可以让前端应用程序更加灵活和可扩展，可以更好地满足不同的业务需求。
2. 可维护性：可以将前端应用程序拆分为多个子应用程序，每个子应用程序都可以独立地开发、测试和部署，从而提高了应用程序的可维护性。
3. 可扩展性：可以让前端应用程序更容易地扩展和升级，可以更快地响应用户需求。
4. 团队协作：可以让不同的团队独立地开发和维护不同的子应用程序，从而提高了团队协作的效率。

微前端的缺点包括：

1. 复杂性：微前端需要使用复杂的技术和工具来实现，需要考虑到不同子应用程序之间的通信和交互，这会增加开发和维护的复杂性。
2. 性能问题：由于微前端需要在运行时加载和卸载模块，因此可能会影响应用程序的性能。
3. 安全问题：由于微前端允许在同一个页面上共存不同的子应用程序，因此可能会带来安全隐患，需要进行严格的安全控制。

总之，微前端是一种新兴的前端应用程序架构，它可以让前端应用程序更加灵活、可维护和可扩展。虽然微前端需要克服一些挑战，但它的优点远远大于缺点，相信在未来的发展中，微前端会越来越受到广泛的关注和应用。

### 主子应用如何通信

主应用处理初始鉴权和菜单信息，获取相关信息之后通过initGlobalState获取setGlobalState和onGlobalStateChange方法传递给子应用，更改主应用里的信息时通过setGlobalState触发基座的状态更新，子应用在mount时通过onGlobalState监听状态变更然后保存在全局的store中。

### 主子应用样式隔离

1. antd相关样式通过使用prefix-cls添加前缀实现
2. 对子应用开启了沙盒模式使用shadow dom。
   1. 改方案缺点是不能逃出该shadow dom，操作modal很难。
3. 子应用内部使用css modules

### 子应用切换样式闪烁问题

由于我们的项目特殊性：

1. 项目迁移应用较少
2. 同一个菜单下配置多个子应用
3. 项目排期紧张

所以采用将css打进js的方法解决

## MV*

### mvc

![image-20230721134454791](/Users/guogang/Library/Application Support/typora-user-images/image-20230721134454791.png)

![image-20230721135043965](/Users/guogang/Library/Application Support/typora-user-images/image-20230721135043965.png)

### mvvm

MVVM架构是一种将视图和模型完全解耦的架构思想，通过数据绑定和命令模式来实现视图和视图模型之间的交互。其核心在于将视图和模型分离，视图只负责渲染用户界面，而视图模型负责处理业务逻辑和数据操作。

在MVVM架构中，模型（model）是一个简单的数据对象，视图模型（viewModel）负责处理业务逻辑和数据操作，视图（view）则负责渲染用户界面。视图和视图模型之间通过数据绑定和命令模式进行交互，从而实现了解耦和职责分离。

## Websocket使用

### 什么是websocket

HTML5开始提供的一种浏览器与服务器进行全双工通讯的网络技术，属于应用层协议。它基于TCP传输协议，并复用HTTP的握手通道。

#### 优点

说到优点，这里的对比参照物是HTTP协议，概括地说就是：支持双向通信，更灵活，更高效，可扩展性更好。

- 支持双向通信，实时性更强。
- 更好的二进制支持。
- 较少的控制开销。连接创建后，ws客户端、服务端进行数据交换时，协议控制的数据包头部较小。在不包含头部的情况下，服务端到客户端的包头只有2~10字节（取决于数据包长度），客户端到服务端的的话，需要加上额外的4字节的掩码。而HTTP协议每次通信都需要携带完整的头部。
- 支持扩展。ws协议定义了扩展，用户可以扩展协议，或者实现自定义的子协议。（比如支持自定义压缩算法等）

### 和http的不同

`HTTP`是单向的，客户端发送请求，服务器发送响应。`WebSocket`是双向的，在客户端-服务器通信的场景中使用的全双工协议，与`HTTP`不同，它以`ws://`或`wss://`开头。它是一个有状态协议，

### 连接原理

WebSocket 是一种基于 TCP 协议的双向通信协议，它可以在客户端和服务器之间建立持久性的连接，实现实时的数据传输和交互。其主要原理如下：

1. 利用 HTTP 建立连接：WebSocket 的连接需要通过 HTTP 请求首先建立握手（Handshaking）过程，该过程类似于普通的 HTTP 请求，但包含了一些特殊的头部字段，例如 Upgrade 和 Connection 等。
2. 建立 TCP 连接：建立 HTTP 连接之后，客户端和服务器之间会建立一个 TCP 连接，并交换协商的加密和压缩参数等。
3. 双向通信：建立好 TCP 连接之后，就可以进行双向通信了。客户端和服务器都可以在任意时刻发送消息，并且不需要发送 HTTP 请求或响应，而是直接通过已经建立好的连接进行数据的传递和处理。
4. 断开连接：当双方其中一方决定关闭连接时，会发送一个特殊的控制帧（Close Frame），告知对方关闭连接。

需要注意的是，在 WebSocket 的连接过程中，由于需要进行 Handshaking 过程，因此第一次连接较慢。同时，在建立连接之后，需要保持长时间的连接状态，因此需要考虑网络稳定性、负载均衡和错误重试等问题，以保证连接的可靠性和稳定性。

WebSocket 是一种基于 TCP 的双向通信协议，通过建立长时间的持久连接来实现客户端和服务器之间的实时数据传输和交互。它在实时性、效率和安全性等方面都有很大的优势，适用于在线游戏、即时聊天、实时监控等领域。

### websocket的handshaking是什么

简单来说就是websocket的建立连接的第一步，在服务器和客户端中交换握手信息，确定双方支持websocket协议。需要交换的头部信息有：`Upgrade`、`Connection`、`Sec-WebSocket-Key` 和 `Sec-WebSocket-Accept` 等，其中 `Sec-WebSocket-Key`、`Sec-WebSocket-Accept` 是握手过程的关键参数，用于实现加密和验证。

### 使用中的问题

1. http和https的协议不同，http必须使用ws协议，https必须使用wss协议

### 自己封装的useWebsocket解决了什么问题

与ahooks的useWebsocket不同的是内部将心跳机制进行了封装，这样无需其他业务在接入的时候都考虑一遍心跳的问题。同时返回给业务的也是制定好的数据结构。大部分业务场景只需要考虑建立连接、接受消息和断开连接即可。

### 心跳是为了解决什么问题

- 为了定时发送消息，使连接不超时自动断线，避免后端设了超时时间自动断线。所以需要定时发送消息给后端，让后端服务器知道连接还在通消息不能断。
- 为了检测在正常连接的状态下，后端是否正常。如果我们发了一个定时检测给后端，后端按照约定要下发一个检测消息给前端，这样才是正常的。如果后端没有正常下发，就要根据设定的超时进行重连。

### 封装心跳思想

每次向服务端发送消息时都会重置当前心跳检测，避免与服务端频繁发送请求时伴随没有必要的心跳检测。

### 重连机制

1. 在每次发送消息的时候都会判断当前连接状态，如果已经断开连接进行重连
2. 在非destory的close时进行重连
3. 在onerror时进行重连

### 其他即时通讯技术

1. 短轮询：客户端不断向服务端发送请求
2. 长轮询：客户端发送请求后服务端挂起请求，等到有新消息时再响应请求
3. SSE：将请求处理为流文件。

## 工程化

### 什么是前端工程化

前端工程化是指将前端开发中的设计、开发、测试和部署等环节进行标准化和自动化，以提高开发效率和代码质量，并降低维护成本。主要分以下流程

1. 模块化
2. 自动化构建
3. 自动化测试
4. 自动化部署
5. 规范化管理

### vite和webpack的区别

Vite 和 Webpack 都是前端打包工具，它们的作用类似，但实现方式和使用方法有所不同。以下是它们之间的一些区别：

1. **构建速度**：Vite 的构建速度比 Webpack 更快，因为 Vite 在开发环境下使用了浏览器原生的 ES 模块加载，而不是像 Webpack 一样使用打包后的文件进行模块加载。在 Vite 中，每个模块都可以独立地进行编译和缓存，这意味着它只需要重新编译修改过的模块，而不是整个应用程序。这使得 Vite 开发起来更加高效。
2. **配置复杂度**：Vite 的配置相对更简单，因为它无需进行大量的配置，只需指定一些基本的选项就可以开始开发。Webpack 的配置更加复杂，需要针对具体项目进行不同的配置，且需要理解各种插件、Loader 等概念。
3. **生态环境**：Webpack 的生态环境更加成熟，在社区中拥有广泛的支持和丰富的插件库。而 Vite 尚处于发展阶段，尽管其已经获得了很多关注，但其生态系统仍然不太完善。
4. **功能特性**：Webpack 是一个功能更加全面的打包工具，支持各种 Loader 和插件，可以处理多种类型的文件和资源。而 Vite 的设计初衷是专注于开发环境下的快速构建，因此其对一些高级特性的支持相对较少。

综上所述，Vite 更适合用于开发环境下的快速构建，而 Webpack 则更适合用于生产环境下的复杂应用程序的打包处理。选择使用哪种工具需要根据具体项目需求进行评估。

### webpack

#### webpack-dev-server

在开发过程中，我们想实时看到代码变更后我们的项目效果时，我们就会启动一个服务来监听代码文件变化，并将新的变更及时的展现在我们的浏览器上，极大的提高了我们的开发效率，这就是`webpack-dev-server`能够实现的。

同时还支持

1. sourcemap
2. overlay
3. proxy
4. hot

等功能。

因为`webpack-dev-server`使用的是`webpack`的`watch`模式进行的编译，当我们更新了代码后，`webpack`是能够监听到代码变化的，代码变化后，`webpack`会再次将我们的项目代码进行打包编译，编译完成后，就会触发`done`钩子函数了。

在上面初始化的时候，我们是在`done`钩子上挂载了回调的。

1. 通过`websocketServer`对客户端进行消息广播，通知客户端项目代码有更新了。

2. 当客户端接收到`websocket`广播的消息后，会触发`reloadApp`方法（`webpack`打包时注入进去的），`reloadApp`会根据广播消息里的更新类型选择是页面更新`liveReload`还是模块更新`hotReload`。
3. 在客户端更新页面时，会去请求类似`c390bbe0037a0dd079a6.hot-update.json`，`main.c390bbe0037a0dd079a6.hot-update.js`这样的两个文件，这两个文件是`webpack` 使用了 `HotModuleReplacementPlugin` 编译时，每次增量编译就会多产出的两个文件， 分别是描述 `chunk` 更新的 `manifest`文件和更新过后的 `chunk` 文件。
4. 拿到这两个增量文件后，再去请求`express`服务器去获取最新编译打包的`bundle.js`。
5. 根据更新类型，选择是两个增量文件和`bundle.js`比对局部更新还是页面更新。

#### 实现打包

1. 首先，我们需要读到入口文件里的内容（也就是index.js的内容）
2. 其次，分析入口文件，递归的去读取模块所依赖的文件内容，生成依赖图
3. 最后，根据依赖图，生成浏览器能够运行的最终代码

##### 处理单个模块

1. 通过fs获取文件内容
2. 通过babel/parser分析模块内容，将入口文件内容转换为ast
3. 通过babel/traverse处理模块内容，对ast做语法转换

##### 递归获取所有模块的信息

根据依赖图，对入口所有的依赖文件做内容分析

##### 生成最终代码

处理代码里的export和require语法，把依赖图对象中的内容，转换成能够执行的代码，以字符串形式输出。 我们把整个代码放在自执行函数中，参数是依赖图对象

#### loader和plugin

##### 区别

- loader 是文件加载器，能够加载资源文件，并对这些文件进行一些处理，诸如编译、压缩等，最终一起打包到指定的文件中
- plugin 赋予了 webpack 各种灵活的功能，例如打包优化、资源管理、环境变量注入等，目的是解决 loader 无法实现的其他事

从整个运行时机上来看，如下图所示：

![img](https://static.vue-js.com/9a04ec40-a7c2-11eb-ab90-d9ae814b240d.png)

可以看到，两者在运行时机上的区别：

- loader 运行在打包文件之前
- plugins 在整个编译周期都起作用

在` Webpack` 运行的生命周期中会广播出许多事件，`Plugin` 可以监听这些事件，在合适的时机通过`Webpack`提供的 `API `改变输出结果

对于`loader`，实质是一个转换器，将A文件进行编译形成B文件，操作的是文件，比如将`A.scss`或`A.less`转变为`B.css`，单纯的文件转换过程

##### 实现loader

loader本质上是一个函数，这个函数会在我们加载一些文件的时候执行。

在函数内部可以通过this获取到文件信息，如果想要编写一个复杂的loader可以通过loader-utils获取配置项内容，进行更多功能的扩展。

同步loader可以直接调用this.callback

异步loader可以通过this.async()获取异步包裹函数，然后实现异步loader。

##### 实现plugin

由于`webpack`基于发布订阅模式，在运行的生命周期中会广播出许多事件，插件通过监听这些事件，就可以在特定的阶段执行自己的插件任务

在之前也了解过，`webpack`编译会创建两个核心对象：

- compiler：包含了 webpack 环境的所有的配置信息，包括 options，loader 和 plugin，和 webpack 整个生命周期相关的钩子
- compilation：作为 plugin 内置事件回调函数的参数，包含了当前的模块资源、编译生成资源、变化的文件以及被跟踪依赖的状态信息。当检测到一个文件变化，一次新的 Compilation 将被创建

如果自己要实现`plugin`，也需要遵循一定的规范：

- 插件必须是一个函数或者是一个包含 `apply` 方法的对象，这样才能访问`compiler`实例
- 传给每个插件的 `compiler` 和 `compilation` 对象都是同一个引用，因此不建议修改
- 异步的事件需要在插件处理完任务时调用回调函数通知 `Webpack` 进入下一个流程，不然会卡住

##### 常见的loader

常见的`loader`如下：

- style-loader: 将css添加到DOM的内联样式标签style里
- css-loader :允许将css文件通过require的方式引入，并返回css代码
- less-loader: 处理less
- sass-loader: 处理sass
- postcss-loader: 用postcss来处理CSS
- autoprefixer-loader: 处理CSS3属性前缀，已被弃用，建议直接使用postcss
- file-loader: 分发文件到output目录并返回相对路径
- url-loader: 和file-loader类似，但是当文件小于设定的limit时可以返回一个Data Url
- html-minify-loader: 压缩HTML
- babel-loader :用babel来转换ES6文件到ES

##### 常见的plugin

![img](https://static.vue-js.com/bd749400-a7c2-11eb-85f6-6fac77c0c9b3.png)

#### 如何提高webpack的构建速度

常见的提升构建速度的手段有如下：

- 优化 loader 配置
- 合理使用 resolve.extensions
- 优化 resolve.modules
- 优化 resolve.alias
- 使用 DLLPlugin 插件
- 使用 cache-loader
- terser 启动多线程
- 合理使用 sourceMap

#### 通过webpack优化前端

通过`webpack`优化前端的手段有：

- JS代码压缩
- CSS代码压缩
- Html文件代码压缩
- 文件大小压缩
- 图片压缩
- Tree Shaking
- 代码分离
- 内联 chunk

#### webpack热更新实现原理

![img](https://static.vue-js.com/adc05780-acd4-11eb-ab90-d9ae814b240d.png)

- Webpack Compile：将 JS 源代码编译成 bundle.js
- HMR Server：用来将热更新的文件输出给 HMR Runtime
- Bundle Server：静态资源文件服务器，提供文件访问路径
- HMR Runtime：socket服务器，会被注入到浏览器，更新文件的变化
- bundle.js：构建输出的文件
- 在HMR Runtime 和 HMR Server之间建立 websocket，即图上4号线，用于实时更新文件变化

关于`webpack`热模块更新的总结如下：

- 通过`webpack-dev-server`创建两个服务器：提供静态资源的服务（express）和Socket服务
- express server 负责直接提供静态资源的服务（打包后的资源直接被浏览器请求和解析）
- socket server 是一个 websocket 的长连接，双方可以通信
- 当 socket server 监听到对应的模块发生变化时，会生成两个文件.json（manifest文件）和.js文件（update chunk）
- 通过长连接，socket server 可以直接将这两个文件主动发送给客户端（浏览器）
- 浏览器拿到两个新的文件后，通过HMR runtime机制，加载这两个文件，并且针对修改的模块进行更新

#### webpack构建流程

从启动到结束会依次执行以下三大步骤：

- 初始化流程：从配置文件和 `Shell` 语句中读取与合并参数，并初始化需要使用的插件和配置插件等执行环境所需要的参数
- 编译构建流程：从 Entry 发出，针对每个 Module 串行调用对应的 Loader 去翻译文件内容，再找到该 Module 依赖的 Module，递归地进行编译处理
- 输出流程：对编译后的 Module 组合成 Chunk，把 Chunk 转换成文件，输出到文件系统

![img](https://static.vue-js.com/d77fc560-a658-11eb-85f6-6fac77c0c9b3.png)

#### babel的原理

babel 的转译过程分为三个阶段，这三步具体是：

- 解析 Parse: 将代码解析生成抽象语法树( 即AST )，即词法分析与语法分析的过程
- 转换 Transform: 对于 AST 进行变换一系列的操作，babel 接受得到 AST 并通过 babel-traverse 对其进行遍历，在此过程中进行添加、更新及移除等操作
- 生成 Generate: 将变换后的 AST 再转换为 JS 代码, 使用到的模块是 babel-generator

## 闭包

### 什么是闭包

一个函数和对其周围状态（lexical environment，词法环境）的引用捆绑在一起（或者说函数被引用包围），这样的组合就是闭包（closure）

也就是说，闭包让你可以在一个内层函数中访问到其外层函数的作用域

### 闭包使用场景

任何闭包的使用场景都离不开这两点：

- 创建私有变量
- 延长变量的生命周期

### react中的闭包

由于react的function comp在使用hook的时候，对所有的hook通过链表存储，所以在一次更新中，就会执行一次function产生一个新的状态，上一个function中的状态只能取到上一个的状态。

所以当我们在setTimeout中使用state时，由于当前function已经被执行完成，所以状态就会保存下来，不能取到最新的state。

## 项目

### 数据填报

#### 封装过哪些自定义hooks

都是针对业务场景的hooks，比如一些useModal、useOss等等。

#### 拖拽方案

1. react-dnd

2. React-beautiful-dnd

3. Dragula

4. 使用h5的draggable

   draggable 属性规定元素是否可拖动。 支持的事件有：

   - ondrag： 当拖动元素时[运行脚本](https://www.zhihu.com/search?q=运行脚本&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra={"sourceType"%3A"article"%2C"sourceId"%3A"643131824"})
   - ondragend： 当拖动操作结束时运行脚本
   - ondragenter：当元素被拖动至有效的拖放目标时运行脚本
   - ondragleave： 当元素离开有效拖放目标时运行脚本
   - ondragover： 当元素被拖动至有效拖放目标上方时运行脚本
   - ondragstart： 当拖动操作开始时运行脚本
   - ondrop： 当被拖动元素正在被拖放时运行脚本

#### 项目优化手段

在建多个表单控件之后，表单内容填写时卡顿严重，因为原来的写法会强制更新所有的表单列表，并且没有用任何的优化手段，所以会造成很多没有必要的render造成性能渲染，主要优化手段有：

1. 通过react.memo减少子组件没有必要的渲染
2. 使用useCallback和useMemo做缓存相关
3. 减少没必要的状态提升
4. 借助手动批量更新

#### 开发中的困难点

本身项目都是比较常规的需求点，因为产品层面考虑也是参考了其他产品的，所以大部分需求都是比较容易实现的，在项目中可能会产生困扰的点主要是

1. 一些UI的非常规设计，因为本身大部分的控件都是基于antd做的二次开发，所以当公司的交互出的东西在一些控件的设计上完全不同于antd时就需要内部封装，要考虑较多东西
2. 一些老控件的改造成本，因为很多代码都是三四年前的使用class写的代码，所以一些自定义hooks就无法使用，在迭代时会写一些不同的处理，尤其是在考虑优化时。

需求层面的困难：

印象比较深的就是级联的实现，因为在新建控件时交互做了完全不同的样式和交互，所以要自己封装一个，需求为：最多四层，每层选中子的时候同时父级要做不同的高亮显示，移除父级所有的子级都要移除，移除子级选中态回到上一父级等这些大致需求，所以在做数据结构设计的时候就参照react的设计，将父与子考虑成链表的结构，每个子都能找到其父级、兄弟、和子级。

### 校园号

#### 部署方案

##### 缓存策略

| 资源                            | 上传方式                                                     | 存储位置                                                     | 缓存策略                                                     |
| ------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| html模板                        | cms发布                                                      | Nginx服务器（运维管理）                                      | cache-control: no-cache etag 协商缓存                        |
| 项目js、css、jpg、png等静态资源 | nbugs-ship-cdn脚本上传                                       | 腾讯云COS+CDN [COS存储桶后台（项目列表）](https://console.cloud.tencent.com/cos/bucket?bucket=front-end-1300113821&region=ap-shanghai) [CDN后台](https://console.cloud.tencent.com/cdn/domains/cdn-3se9nsf9?tab=cache) | 1浏览器本地缓存30天 Cache-Control:  max-age=2592000 2cdn节点缓存30天 3cos默认支持etag 协商缓存 |
| auth-loading.js等鉴权资源       | nbugs-front-tool项目管理 nbugs-ship-cdn脚本上传[自定义文件上传-腾讯云cos](https://www.yuque.com/nbugs-fe/fe-team/ogygv1) | 腾讯云COS+CDN [COS存储桶后台(鉴权js)](https://console.cloud.tencent.com/cos/bucket?bucket=front-end-1300113821&region=ap-shanghai&path=%2Fnbugs-cdn%2Fjs%2F0.0.1%2F) [CDN后台](https://console.cloud.tencent.com/cdn/domains/cdn-3se9nsf9?tab=cache) | 1浏览器本地缓存10分钟 Cache-Control:  max-age=600 2cdn节点缓存30天，脚本上传通过接口手动刷新 3cos默认支持etag 协商缓存 |

###### 打包部署流程

1. deploy打包命令
2. 脚本库将项目文件发布到腾讯云cos，腾讯云同步到cdn。
3. 通过cms配置nunjucks模板，点击发布，将html文件保存在nginx服务器上，通过script加载cdn资源，测试资源去测试cdn上取，线上资源通过会有cdn加速节点进行优化获取资源，获取不到资源时回源源站。
4. 前端通过容器激活的activeRule获取子应用资源，通过nginx解析获取相关html。

##### 其他部署方案

[前端部署](https://juejin.cn/post/7017710911443959839)

### 私有化

#### 容器和子应用如何同一域名

通过端口号做区分

#### 跨技术栈接入第三方平台

[与第三方合作技术整改](https://www.yuque.com/inuyasha/fe-team/xpeayh)

## 自我介绍

面试官你好，我叫郭钢，今年28岁，目前就职于浙江小虫科技有限公司，主要负责多个PC和移动应用的迭代、业务组件的维护和基建部分的开发，目前负责项目的私有化改造及微前端架构的历史问题改造。

## 简述印象最深(最复杂)的一个项目

如果是最复杂就说：因为受限于业务场景，我们的大部分业务实际上都到不了特别复杂的程度。

相对复杂一些和印象深一些的是目前我正在负责的项目私有化，因为需要涉及到与第三方的合作，将我们的移动端代码托管到他们的平台部署。因为他们是vue项目，而我们是react项目就需要首先解决这个问题(然后说明是怎么解决的)，对于pc端项目就需要将微前端架构重新改造以适应他们的平台，同时由于后端部分服务不能迁移，所以一些类如菜单部分就需要前端管控，所以就需要对原来的一整套逻辑重新梳理，从容器到子应用需要涉及的改造进行详细的罗列，将整个流程梳理清晰。同时为了后续其他业务再次私有化时做最小化的修改还需要把新的流程做的足够通用化和可扩展，并提供完整的改造文档做组内分享。

## 小程序

Taro 是一个多端统一开发框架，可以使用一套代码编译成微信小程序、支付宝小程序、百度智能小程序、字节跳动小程序、QQ 小程序、快应用、H5 等多个平台的应用。

Taro 的实现原理主要基于以下几个方面：

1. **JSX 转换**：Taro 使用 Babel 插件将类似 HTML 的语法转换为 React 组件。在编译过程中，Taro 还会对 JSX 语法进行优化和压缩，以避免生成不必要的代码。
2. **多端适配**：Taro 通过封装原生 API 和提供不同的 Polyfill 实现多端适配。例如，在微信小程序中，Taro 封装了 wx 对象，使得可以使用类似 React Native 的组件化开发方式；在 H5 中，Taro 则提供了针对浏览器的 Polyfill。
3. **跨端样式处理**：Taro 通过 CSS Modules 技术和 PostCSS 插件来处理 CSS 样式。在编译过程中，Taro 会将样式文件转换为 JavaScript 对象，并按需导入到组件中。同时，Taro 提供了 @import 指令或 scss 语法等方式来支持复杂的样式表达。
4. **构建系统**：Taro 使用 webpack 构建工具来打包编译后的代码，并提供了一系列开箱即用的插件、规则和配置项，例如自动化导入组件、静态资源压缩、TypeScript 支持等。
5. **运行时性能优化**：Taro 在运行时对代码进行了一些优化，例如使用字典树实现 JSX 解析、避免使用内置事件监听器、减少对原生 API 的调用等方式来优化性能。

Taro 利用 Babel、React、Webpack 等技术，通过封装原生 API 和提供不同的 Polyfill 实现了多端适配，同时也支持复杂的样式表达和自动化导入组件等特性。这些技术的应用使得 Taro 框架在性能、可维护性、跨平台等方面都表现出色。

## taro2.x和taro3最大区别是什么

`Taro 2.x` 和 `Taro 3` 的最大区别可以总结为以下几个方面：

1. **编译方式**：Taro 2.x 使用 Gulp 构建工具进行编译，而 Taro 3 改为使用 Webpack 进行构建。这使得 Taro 3 在编译速度、可扩展性、构建配置等方面有了更好的表现。
2. **React 版本升级**：Taro 2.x 使用的是 React 16 版本，而 Taro 3 升级到了 React 17 版本。React 17 引入了一些新特性，例如以初始渲染器为基础的事件处理、重新设计的事件系统等，从而提高了性能和稳定性。
3. **API 改进**：Taro 3 对 API 进行了改进，并引入了新的特性。例如，在 JSX 中可以使用 class 关键字来定义 CSS 样式；增加 useReady 钩子函数在小程序生命周期 onReady 被触发时执行；引入了快应用和 H5 等新平台的支持等。
4. **插件机制**：Taro 3 引入了插件机制，使得开发者可以通过插件实现更多的功能和特性，例如对 TypeScript 支持的扩展、国际化支持等。
5. **性能优化**：Taro 3 在性能方面进行了优化，例如使用虚拟 DOM 进行局部更新，减少对原生 API 的调用等。同时，Taro 3 可以根据平台的不同生成更小的代码包。

`Taro 3` 引入了一些新特性和优化，并提高了性能、可扩展性和稳定性。

如果需要使用 Taro 框架开发多端应用，建议选择 Taro 3。

## hr面问题整理

### 自我介绍

面试官你好，我叫郭钢，今年28岁，目前就职于浙江小虫科技有限公司，在公司任职期间，我主要负责多个PC和移动应用的迭代、业务组件的维护和基建部分的开发，目前负责项目的私有化改造及微前端架构的历史问题改造。在日常工作中我善于做文档总结输出，然后对组内做相关分享，在团队合作中会从做好产品方向出发做出积极响应，在项目迭代中会快速培养owner意识。

### 为什么选择我们公司

在这次公司选择中，我主要看重的有三点，第一点是个人成长空间，第二点是公司发展前景，第三点是薪资。综合这三点之后我选择了咱们公司。

### 你最大的缺点是什么

有时候我是一个完美主义者,在做任何事之前我一定要确定每个细节是否已准备妥当,所以在一些

### 你对薪资的要求

目前19*14 会有一个月奖金 所以在28左右

期望base在24左右

### 工作中难以和同事相处怎么办？

本身是比较随和的性格，所以这种情况应该比较少。但是工作中不可避免会有这种情况出现，那就具体问题具体解决嘛，因为大家都是为了让项目变得更好，所以会从这一根本点触发去解决问题，如果实在不行只能向上抛出了

### 你觉得这个工作有什么困难？

会困难的点就是一些未知技术方案的实现吧，需要调研过程给出足够详细的技术方案

### 除了本公司，你还投了哪些企业？

投了几个比较知名一点的企业，目前有的待二面有的待hr面

### 为什么不直接投阿里集团？

因为之前有问过朋友目前阿里本身对硬性条件要求还比较高，所以目前学历可能是比较难的点。

### 你对这个行业以及职位有哪些理解

因为前端是属于技术里最直接接触用户的人，所以最重要的考虑点就是交互体验问题，一个产品的功能点都会在产品需求里体现的很清楚，但是怎么让用户最少理解成本，最简洁的操作步骤。

### 未来规划

首先在业务迭代的过程中提升个人技术和效率，把自己的成长和技术提升能普及到团队中可能会没有了解过该技术的人。然后扩展自己的眼界和技术广度和深度，对一些项目接入新技术的使用，提高团队效率，业余之时也会想办法提高一下自己的管理能力和产品意识，能更好的做出好的产品。

### 你还有什么问题吗

1. 公积金？

2. 转正多久？

3. 负责的内容？

4. 绩效考核？

5. 部门大小？日常作息？

   
